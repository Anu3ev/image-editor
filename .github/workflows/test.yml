name: Tests

on:
  pull_request:
    branches: [ master, main, refactor ]
  push:
    branches: [ master, main, refactor ]

permissions:
  contents: read
  statuses: write
  issues: write
  pull-requests: write

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18, 20]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint
        continue-on-error: true

      - name: Run tests
        run: npm run test:ci

      - name: Generate test coverage
        run: npm run test:coverage
        if: matrix.node-version == 20

      - name: Upload coverage to artifact
        uses: actions/upload-artifact@v4
        if: matrix.node-version == 20
        with:
          name: coverage-report
          path: coverage/
          retention-days: 30

      - name: Comment PR with test results
        uses: actions/github-script@v7
        if: github.event_name == 'pull_request' && always()
        with:
          script: |
            const fs = require('fs');
            
            // Проверяем статус тестов
            const testStatus = '${{ job.status }}';
            const nodeVersion = '${{ matrix.node-version }}';
            const emoji = testStatus === 'success' ? '✅' : '❌';
            
            let comment = `${emoji} **Результаты тестов (Node.js ${nodeVersion})**\n\n`;
            
            if (testStatus === 'success') {
              comment += '🎉 Все тесты прошли успешно!\n\n';
              
              // Добавляем информацию о покрытии, если есть
              if (nodeVersion === '20') {
                try {
                  const coverageExists = fs.existsSync('coverage/coverage-summary.json');
                  if (coverageExists) {
                    const coverage = JSON.parse(fs.readFileSync('coverage/coverage-summary.json', 'utf8'));
                    const total = coverage.total;
                    
                    comment += '📊 **Покрытие кода:**\n';
                    comment += `- Строки: ${total.lines.pct}%\n`;
                    comment += `- Функции: ${total.functions.pct}%\n`;
                    comment += `- Ветки: ${total.branches.pct}%\n`;
                    comment += `- Утверждения: ${total.statements.pct}%\n\n`;
                  }
                } catch (e) {
                  console.log('Coverage info not available');
                }
              }
            } else {
              comment += '💥 Некоторые тесты упали!\n\n';
              comment += '⚠️ **Внимание**: Мерж заблокирован до исправления тестов.\n\n';
              comment += 'Проверьте логи выше для получения подробностей.';
            }
            
            // Отправляем комментарий только для Node.js 20 чтобы не дублировать
            if (nodeVersion === '20') {
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: comment
              });
            }
